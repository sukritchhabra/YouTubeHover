var defaultSettings = {
    delayOnHover: {
        hoverDelay: 1
    },

    skipIntervals: {
        enabled: "enabled",
        format: "playing",
        increment: "20",
        incrementFactor: "50",
        quality: "small"
    }
};