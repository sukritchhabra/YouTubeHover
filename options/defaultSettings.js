var defaultSettings = {
    delayOnHover: {
        hoverDelay: 1
    },

    skipIntervals: {
        enabled: "disabled",
        format: "playing",
        increment: "5",
        incrementFactor: "400",
        quality: "large"
    }
};